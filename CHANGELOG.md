# 版本更新日志

## v3.11.4 (2026-09-23) - 查证「一键服用不会误吞还魂丹」+ 修复显示误报 🔎

**🎯 玩家提问**

> 「顺便查一下会不会有『服用丹药』把还魂丹服用的 BUG?」

### 查证结论：不会误吞（真实库副本 + 真实 handler 全链路实测）

| 场景 | 实测结果 |
|------|----------|
| 背包有还魂丹 + `一键服用` | 还魂丹 **1 → 1 不变**，输出「还魂丹×1（需手动使用）」 |
| 非元神状态 `服用丹药 还魂丹` | 拒绝且**不消耗**：「你并未处于元神状态，无需使用还魂丹」 |
| 元神状态 `服用丹药 还魂丹` | 消耗 1 个 → 完美复活、修为无损 |
| 元神状态 `使用还魂丹` | 消耗 1 个 → 完美复活、修为无损 |
| 元神状态 `一键服用` | 指令被元神白名单拦下，还魂丹**不会被消耗** |
| 复活流程失败 | 丹药自动退还（`use_revival_pill` 原有兜底） |

保护来自两处，缺一不可：

1. `config/utility_pills.json`：还魂丹声明 `manual_only: true`，`PillManager.use_all_pills()` 识别后直接跳过；
2. `handlers/revival_handler.py`：还魂丹复活走 `use_revival_pill()` 独立链路
   （先查元神状态 → 再查背包 → 先扣丹后复活，复活失败自动退还）。

`handlers/pill_handler.py` 的 `_is_revival_pill()` 也会把 `服用丹药 还魂丹` 提前路由到复活链路，
不会落进普通丹药的批量消耗分支。

### 🐛 顺带修掉一个真实的显示误报

| 位置 | 修复前 | 修复后 |
|------|--------|--------|
| `我的信息` →「还能吃几个」 | `还魂丹×2 → 可服用 2 个` | `还魂丹×2 → 暂不可服用（需手动使用）` |

**根因**：`PillManager.get_consumption_limit()` 未处理 `manual_only` —— 该标记此前只在
`use_all_pills()` 的循环里被单独判断，于是所有依赖 `get_consumption_limit()` 的展示
（`我的信息` 的丹药进度行）都会把还魂丹当成普通可服丹药，与实际行为不一致，
也正是这次「会不会被误吞」疑虑的来源。
**修复**：在 `get_consumption_limit()` 内统一拦截 → `(0, "需手动使用")`，展示口径与消耗逻辑对齐。

## v3.11.3 (2026-09-23) - 修复「一键炼化把还魂丹材料炼了」🐛

**🎯 玩家反馈**

> 「一键炼化现在都会把还魂丹的材料炼化了，这不让人很难受吗。。」

### 根因：v3.11.0 把丹药原料当成普通事件材料加进了炼化白名单

v3.11.0 为了让世界事件掉落不再「压箱底」，把 14 种事件材料补进 `handlers/storage_ring_handler.py`
的 `MATERIAL_REFINING_VALUES`。问题是这份白名单里混进了 6 种**丹药配方原料**：

| 材料 | 原料所属丹药 | 单份炼化收益 |
|------|--------------|--------------|
| 九转仙草 | 还魂丹（5星） | 灵石 32,000 + 修为 128,000 |
| 太古龙骨 | 还魂丹 | 灵石 24,000 + 修为 96,000 |
| 灵髓精华 | 还魂丹 / 悟道丹 | 灵石 8,000 + 修为 32,000 |
| 月华精粹 | 还魂丹 | 灵石 6,000 + 修为 24,000 |
| 嗜血草 | 狂暴丹（3星） | 灵石 1,200 + 修为 4,800 |
| 妖兽内丹 | 狂暴丹 | 灵石 2,000 + 修为 8,000 |

而 `一键炼化` 的实现是把整个白名单交给 `refine_storage_materials_batch()` **无差别全炼**，
于是玩家攒了几天、专门为还魂丹准备的九转仙草，一条指令就全变成了灵石。

### 修复

1. **保护名单从配方配置动态推导**：`ConfigManager.get_recipe_material_map()` 读取 `config/alchemy_recipes.json`，
   产出「材料名 → 需要该材料的丹药名列表」（带懒加载缓存）。
   以后新增/调整丹方，保护名单自动跟随，不会再出现「加了配方却忘了加保护」。
2. **`一键炼化` 自动跳过**：只把非配方原料交给批量炼化，并在结果里逐条列出：
   ```
   🔒 已自动跳过丹药配方原料（不炼掉你的炼丹材料）：
     · 九转仙草×7（还魂丹）
     · 灵髓精华×3（还魂丹 / 悟道丹）
   💡 确实要炼化请显式执行：炼化材料 材料名 数量
   ```
   若全是配方原料而没有可炼化材料，也会给出同样的跳过清单而不是冷冷一句「没有可炼化的材料」。
3. **手动路径保留**：`炼化材料 <材料名> <数量>` 依旧可以炼化配方原料（玩家显式指定即视为知情），
   但成功消息会追加警告：「⚠️ 注意：【九转仙草】是【还魂丹】的原料，已被你显式炼化」。
4. **提示可见化**：
   - `储物戒` 列表标签：「可炼化⚗️」→ 配方原料显示为「**配方材料🔒**」
   - `炼化图鉴`：配方原料前加 🔒 并附所属丹药，另单列「🔒 丹药配方原料（一键炼化会自动跳过）」清单
   - `/修仙帮助`、`储物戒帮助`、`世界事件帮助` 文案同步

### 验证（未启动 AstrBot）

真实数据库副本 + 真实 handler（`StorageRingHandler.handle_refine_all`）全链路：

| 材料 | 炼化前 | 一键炼化后 |
|------|--------|-----------|
| 玄铁 | 5 | 0（已炼化：灵石+1,500 修为+6,000） |
| 混沌精华 | 3 | 0（已炼化：灵石+24,000 修为+96,000） |
| 仙灵草 | 2 | 0（已炼化：灵石+120,000 修为+480,000） |
| 九转仙草 | 7 | **7（🔒跳过）** |
| 太古龙骨 | 2 | **2（🔒跳过）** |
| 灵髓精华 | 3 | **3（🔒跳过）** |
| 月华精粹 | 2 | **2（🔒跳过）** |
| 嗜血草 / 妖兽内丹 | 2 / 1 | **2 / 1（🔒跳过）** |

另跑 `炼化材料 九转仙草 1` 确认手动路径可用并输出配方原料警告（7 → 6，收益正常入账）。

## v3.11.2 (2026-09-23) - 修复「装备饰品 = 装备被吞」🐛

**🎯 玩家反馈**

> 「`=卸下 戮仙剑阵` 回我『未找到装备：戮仙剑阵』，可储物戒里也翻不到剑阵了 —— 给我吞了？」

玩家没说错，饰品确实被吞了，而且**自 v3.9.3 饰品栏上线以来，任何一件饰品只要装备过一次就必然消失**。

### BUG 1（根因）：`update_player()` 写库列漏了 `accessory`

`players` 表有 `accessory` 列（v38 迁移加的「饰品装备系统」），`Player` 模型有该字段，读取时也在 `PLAYER_FIELDS` 里，
唯独 `data/data_manager.py:update_player()` 的 `UPDATE players SET ...` **没有这一列**。
于是「修仙装备 饰品」的真实时序是：

```
物品已从储物戒取出（写库） → player.accessory = "戮仙剑阵"（仅内存） → update_player() 整行覆盖，丢弃该字段
→ 提示「✅ 已装备饰品【戮仙剑阵】（帝品）」
```

结果物品**既不在储物戒里、也不在装备栏里**，玩家随后 `卸下 戮仙剑阵` 自然得到「未找到装备」。
饰品属性也因此从未真正计入过战力。

**修复**：`update_player` 补上 `accessory = ?` 与对应参数（`create_player` 的 INSERT 一并补齐保持同步）。
顺手用脚本核对 `Player` 全部 51 个字段与 `update_player` 的 SET 列，缺失字段 **1 → 0**。

### BUG 2：`卸下 饰品` 会静默销毁饰品

`handlers/equipment_handler.py` 的「卸下」为了把旧装备放回储物戒，在调用前自己维护了一份槽位→物品名映射，
而这份映射**只写了武器/防具/主修心法，漏了饰品**。`EquipmentManager.unequip_item()` 已经清空 `player.accessory`
并落库，handler 却拿不到物品名，于是既不回存也不提示，还回了一句「✅ 已卸下饰品【X】」。

**修复**：删掉 handler 里这份重复映射，卸下与回存统一由 `EquipmentManager` 负责——
**先把装备存回储物戒，收纳成功才清空装备栏**；收纳失败则原样保留装备并说明原因。

### 顺带修复的同类问题

| # | 问题 | 修复 |
|---|------|------|
| 3 | `卸下 <装备名>` 一律「未找到装备」（本次玩家就是这么撞上的） | 支持按槽位或**直接写装备名**卸下：`卸下 戮仙剑阵`、`卸下 饰品` 均可；错误提示列出可用槽位 |
| 4 | `我的装备` / `我的信息` 的装备段没有饰品行，玩家看不到身上的饰品 | 两处补上「饰品」栏位显示 |
| 5 | 储物戒满时替换装备会吞旧装备（先覆盖装备栏、再尝试回存，回存失败只给 ⚠️ 提示） | 改为**先收纳旧装备、成功才替换**；储物戒满时拒绝替换并提示清理空间，新装备退回储物戒 |
| 6 | 装备失败返还储物戒失败时静默忽略 | 改为记录 `logger.error` 并在消息里提示联系管理员 |

### 新增/变更的指令行为

| 指令 | 变化 |
|------|------|
| `卸下 <槽位\|装备名>` | 🆕 槽位支持 `武器 / 防具 / 饰品 / 心法 / 功法`；也可直接写已装备的物品名 |
| `修仙装备 <饰品名>` | 现在会真正落库并计入战力（此前装备即丢） |

### 代码结构

- `core/equipment_manager.py`：新增 `EQUIPMENT_SLOTS`（item_type → Player 字段 + 中文名）与 `SLOT_ALIASES`（卸下参数别名），
  新增 `resolve_equipped()`；装备/卸下共用同一套映射，以后新增装备栏位只需补一行，不会再出现 handler 与 core 各写一份而漏项
- 移除 `EquipmentManager._store_old_equipment()`（其「失败只提示不回滚」的写法正是吞装备的温床）

**🧪 验证方式**（未启动 AstrBot）：用 `/tmp/xtverify` 软链包环境加载真实代码，
以玩家提供的 `xiuxian_data_lite.db` 副本跑通「获得 → 修仙装备 → 我的装备 → 卸下 饰品 → 按装备名卸下 → 储物戒满时替换」全链路；
实测装备【戮仙剑阵】后物伤 154135 → 154935（+800）、物防 110122 → 110322（+200），与 `equip_effects` 一致，说明饰品属性已真正计入战力；
并在 `git HEAD` 版本上复现修复前行为（`accessory` 恒为空、旧装备被覆盖消失）作为对照。

> 💡 **已受影响的玩家**：饰品栏上线以来装备过的饰品都已被吞。可按玩家反馈的物品名补发回储物戒（正常入库即可，无需改库）。

## v3.11.1 (2026-09-22) - 修复「看不到战斗过程」：元神拦截 + 战报误报 🐛

**🎯 玩家反馈**

> 启用了「可见战斗过程」，事件播报也确实是 4 回合逐段推进的，但
> `=世界事件战报` 要么被元神状态拦下，要么回一句「该场事件未启用可见战斗过程」。**怎么还是不能看到战斗过程？**

复盘后是两个独立 BUG 叠加，都被这一次反馈同时踩到：

### BUG 1：元神状态把「世界事件战报」拦掉了

`handlers/utils.py` 的 `SOUL_STATE_ALLOWED_COMMANDS`（元神状态下可用的指令白名单）里
只登记了 `世界事件` 与 `世界事件战绩`，**漏掉了 v3.10.0 新增的两条只读指令**：

- `世界事件战报`（`CMD_WORLD_EVENT_BATTLE`）
- `我的世界事件奖励`（`CMD_WORLD_EVENT_REWARDS`）

`_match_command()` 要求「完全相等或后接空格」，所以 `世界事件战报` 不会被 `世界事件` 这条前缀放行，
直接落到兜底分支被拒。结果就是最荒谬的时序：

```
你在世界事件里阵亡 → 变成元神状态 → 想看自己是怎么死的 →「⚠️ 元神状态无法进行此操作，请先复活！」
```

而**阵亡那一刻恰恰是最想看战报的时候**。

**修复**：把上述两条补进元神白名单。顺手用脚本核对了全部 151 条注册指令，
把同样属于「纯只读却被漏掉」的 `传承信息` / `传承排行` 一并补上（只读指令缺失：4 → 0）。
禁用的仍是真正会改状态/资源的行为指令（闭关、加入世界事件、探索秘境、炼化材料等），行为不变。

### BUG 2：战报误报「该场事件未启用可见战斗过程」

`format_battle_report()` 原来只取 `event_id` 最大的一场：

```sql
ORDER BY e.event_id DESC LIMIT 1
```

如果玩家**最近参战的那一场**恰好早于「可见战斗过程」上线（或当时功能关着），
就会把开着的功能报成没开 —— 即使配置明明开着、更早的场次也有完整逐回合记录。
本例中玩家最近参战的是旧的「魔修入侵」，而去查的人又不是刚结束那场「上古遗迹现世」的参战者，
叠加起来就成了「功能明明开着，却被告知没开」。

**修复**：向前扫描最近 `BATTLE_REPORT_SCAN_LIMIT`（20）场，优先回放**真的有逐回合记录**的那一场，
并且把「为什么不是最近那场」说清楚：

| 情况 | 提示 |
|------|------|
| 回放的是更早一场（事件名不同） | 💡 你最近一场【魔修入侵】没有逐回合记录，已为你回放上一场有记录的【上古遗迹现世】 |
| 回放的是更早一场（同名事件） | 💡 你最近一场【X】没有逐回合记录，已为你回放更早的一场（同名事件） |
| 全部都没有记录、但功能**开着** | 💡 逐回合记录只对「开启可见战斗过程之后新开的事件」生效，历史事件不会补录 |
| 功能**关着** | 💡 该场事件未启用可见战斗过程　• 开启方式：插件配置 → 世界事件配置 → 启用可见战斗过程 |

最后一条还把「配置没开」和「历史事件不补录」区分开了 —— 原来的文案把这两种情况混为一谈，
正是它误导玩家以为配置没生效。

**🧪 验证方式**（未启动 AstrBot）

- **元神白名单**：隔离加载 `handlers/utils.py`，对 18 条指令断言放行/拦截，
  含带斜杠前缀、带参数、以及「服用丹药 回生丹 应被拦、服用丹药 还魂丹 应放行」的边界
- **战报回归**：真实 sqlite + 真实 `WorldEventManager` 跑端到端 —— 创建 → 报名 → 开战 →
  逐回合推进 → 结算 → 查战报/奖励，覆盖「生还者 / 阵亡者（元神）」双视角，
  以及「最近一场无记录」「全部无记录」两个回归场景
- **强制阵亡路径**：把每回合死亡率置 1 验证阵亡者视角（报告显示 `💀 此战阵亡`、
  `结算结果：💀 阵亡`，且修为按 2.5% 折扣扣除、奖励按 50% 发放）
- **死亡率守恒复验**：5 档死亡率 × 3/4/5 回合共 15 组、每组 6 万次蒙特卡洛，
  实测存活率与理论值偏差均 < 0.4%，确认本次改动未触碰 v3.10.0 的死亡率模型

**📦 数据库变更**：无
**🔧 新增配置**：无（仅新增模块内常量 `BATTLE_REPORT_SCAN_LIMIT = 20`）

---

## v3.11.0 (2026-09-22) - 世界事件奖励重构：让「复活道具副本」值得去 🌍

**🎯 玩家反馈**

> 「你想想我们参加世界事件最主要的目的是啥 —— 是获取复活道具的材料。但明明是获取复活道具的副本偏偏有死亡风险，
> 那我直接不参加就不死了……喜欢我顶着死亡风险得到俩魔核吗」

复盘确认三个真问题（外加一个隐藏问题）：

| # | 问题 | 根因 | 修复 |
|---|------|------|------|
| 1 | 收益没有参与动机 | 修为/灵石是固定区间、不随境界成长：合体期打高阶事件只有 5-10 万修为，而同境界秘境「混沌神殿」给 80-180 万，**差 10-20 倍** | 奖励改为按境界成长 |
| 2 | 复活道具材料刷不出来 | 还魂丹需 九转仙草×1 + 太古龙骨×1 + 灵髓精华×3 + 月华精粹×2，而单场每种材料只有 25%~40% 概率掉 **1 件**；集齐一颗丹期望 **9.2 场** | 材料数量区间 + 保底 |
| 3 | 冒风险还倒亏修为 | 阵亡只拿 20% 奖励，且照样扣 5% 修为（上亿修为的号死一次 = 上百万，比一次秘境收益还高） | 阵亡保留率与修为损失双降 |
| 4 | **材料本身没有消耗途径**（隐藏） | 魔核 / 魔晶 / 妖兽内丹等既不是任何丹方原料，也不在 `MATERIAL_REFINING_VALUES` 炼化白名单里 —— 「俩魔核」确实是纯废料 | 14 种事件材料纳入炼化 |

**✅ 具体改动**

### 1. 奖励随境界成长（`managers/world_event_manager.py`）

- 新增 `reward_ref_level`（模板的基准境界）与 `reward_level_growth`（该难度的成长系数）
- 模板里的 `exp` / `spirit_stone` 语义变为「**基准境界处**的数值」；
  玩家每高出基准 1 个小境界，修为/灵石 ×`reward_level_growth`
- 各难度按**该区间秘境的经验曲线**单独标定成长系数：低阶 1.15 / 中阶 1.40 / 高阶 1.30 / 史诗 1.60
- 低于基准境界不减益（保底 ×1.0），`reward_max_multiplier`（默认 40）封顶兜底

| 事件 | 玩家境界 | 事件修为均值 | 同境界秘境 | 倍率 | 材料件/场 |
|------|----------|-------------|-----------|------|----------|
| 低阶·妖兽围城 | 炼气二层 | 4,194 | 1,000-3,000 | 2.10x | 1.41 |
| 低阶·妖兽围城 | 筑基后期 | 9,769 | 5,000-12,000 | 1.15x | 1.41 |
| 中阶·魔修入侵 | 金丹初期 | 32,686 | 8,000-20,000 | 2.33x | 2.13 |
| 中阶·魔修入侵 | 元婴后期 | 89,180 | 50,000-120,000 | 1.05x | 2.12 |
| 高阶·上古遗迹 | 化神初期 | 681,220 | 50,000-120,000 | 8.01x | 3.87 |
| 高阶·上古遗迹 | 合体后期 | 1,951,176 | 800,000-1,800,000 | 1.50x | 3.81 |
| 史诗·域外天魔 | 大乘初期 | 6,016,514 | 2,000,000-6,000,000 | 1.50x | 10.55 |
| 史诗·域外天魔 | 地仙 | 39,117,993 | 15,000,000-40,000,000 | 1.42x | 10.50 |

### 2. 材料掉落：数量区间 + 每场保底

- `materials` 条目新增 `count`（支持 `int` 或 `[min, max]`）：灵髓精华 ×2~3、史诗档 九转仙草/太古龙骨 ×2~3、灵髓精华 ×3~5、月华精粹 ×2~4
- 新增 `reward_guaranteed_material`（默认 1）：从该事件材料池补足，保证**每场至少 1 件材料**
  （此前低阶事件有约 35% 概率空手）
- 效果：集齐一颗还魂丹材料的中位场次 **9 场 → 4 场**，均值 **9.16 场 → 4.48 场**

### 3. 阵亡代价下调

- `death_penalty_reward_rate` 默认 **0.2 → 0.5**（阵亡者保留一半奖励）
- 新增 `death_exp_loss_scale`（默认 0.5）：`core/death_manager.py:apply_death()` 新增同名
  可选参数，世界事件阵亡只承担**常规一半的修为损失**（金丹以上 5% → 2.5%，
  低阶劫后重生保留 70% → 85%）。**境界 / 元神状态 / 彻底陨落判定完全不变**；
  不传参时行为与 v3.10.0 完全一致（突破走火入魔不受影响）
- 材料掉落不受阵亡惩罚影响（沿用原行为，未做削弱）

### 4. 世界事件材料纳入炼化（`handlers/storage_ring_handler.py`）

- 14 种材料加入 `MATERIAL_REFINING_VALUES`：妖兽皮毛 / 邪修令牌 / 血灵珠 / 妖兽内丹 / 嗜血草 /
  魔晶 / 魔核 / 月华精粹 / 灵髓精华 / 太古龙骨 / 九转仙草 / 混沌石 / 天魔精华 / 仙灵草
- 取值口径与既有一致：灵石 ≈ items.json 价格 × 0.4，修为 = 灵石 × 4
- 同时补进 `ITEM_CATEGORIES["材料"]`（物品索引缺失时也能正确归类）

### 5. 收益透明化（新增能力）

- `WorldEventManager.describe_reward_preview()`：按玩家境界给出「生还收益」与
  「折算风险后的期望 = 生还概率 + 阵亡概率 × 阵亡保留比例」，并附境界加成与材料保底说明
- 「世界事件」面板（`handlers/world_event_handlers.py:handle_show` 传入 player）与
  报名成功回执均展示该预览；`describe_reward_preview` 为公开方法，便于后续复用到广播文案

**🐛 BUG 修复**

- 「我的世界事件奖励」在**同名材料多件**时逐行重复列出，改为按名称聚合成「名称×N」
- 同指令的「储物戒已满未入库」判定用 `item not in stored` 做成员判断，同名物品
  「存了一个、漏了一个」时会误报为已入库；改为 `Counter` 数量差集
- 事件详情面板对 `max_level` 超出境界表的模板（史诗档 `max_level: 36`）显示出生造名词
  「境界36」；`_get_level_name()` 现钳到境界表最后一档（不影响境界压制/免疫判定逻辑）

**🔧 新增可调参数**（面板「世界事件配置」+ `config/world_events.json`）

| 参数 | 默认 | 说明 |
|------|------|------|
| `reward_level_scaling` | true | 修为/灵石是否随境界成长 |
| `reward_level_growth` | 1.35 | 全局成长系数（模板内 `reward_level_growth` 可单独覆盖） |
| `reward_max_multiplier` | 40.0 | 境界成长倍率上限 |
| `reward_guaranteed_material` | 1 | 每场至少掉落的材料件数（0=关闭保底） |
| `death_penalty_reward_rate` | 0.5 | 阵亡奖励保留比例（原 0.2） |
| `death_exp_loss_scale` | 0.5 | 事件阵亡的修为损失折扣（1.0=与常规死亡一致） |

**📦 数据库变更**：无（本次为纯数值/逻辑调整，`LATEST_DB_VERSION` 保持 v41）

**🧪 验证方式**：未启动 AstrBot，隔离加载 `managers/world_event_manager.py` 与
`core/death_manager.py`，用真实 `config/world_events.json` / `level_config.json` 做
3000~20000 次蒙特卡洛实抽与死亡结算断言（收益倍率、材料保底、阵亡折扣、旧行为回归）。

---

## v3.10.0 (2026-09-22) - 世界事件可见战斗过程 + 死亡率模型重做 ⚔️

**🎯 核心改进**

### 1. 世界事件：从「黑盒」到「可见的战斗过程」

**问题**：报名 → 30-90 分钟静默 → 结果名单。玩家全程只掷一次骰子，
看不到任何过程；连 AI 生成的「惨烈战斗」和自己也毫无因果关系
（旧版 prompt 明确要求不出现玩家名字），只能理解为「系统弹了段文案」。

**改进**：`报名 → 可见的战斗过程 → 结果`

- ✅ **分回合推进**：开战后按时长拆成 3~5 个回合（≤45 分钟 3 回合 / ≤60 分钟 4 回合 / 更长 5 回合），
  每回合独立掷骰并实时播报；最大消息空档从 30-90 分钟压到 7-22 分钟
- ✅ **回合播报内容**：敌方首领气血、本轮主力（输出占比）、被重创者、阵亡名单（含死因）
- ✅ **总死亡率严格守恒**：每回合死亡率取 `p_node = 1 - (1 - p) ** (1 / N)`，
  N 次独立掷骰后总死亡率与旧版完全一致（蒙特卡洛实测 1/3/4/5 回合 × 15%~70%，偏差均 < 0.3%），
  v3.9.1 / v3.9.2 建立的境界压制与战力减免平衡无需重调
- ✅ **新增指令** `/世界事件战报`：逐回合回放（个人视角），看自己每回合做了什么、战意剩多少、在哪一回合倒下
- ✅ **战意模型**：战意由每回合「生存余量」推导（roll 越贴近死亡线掉得越多），
  因此「战意见底 ⇔ 离死亡一步之遥」，不会出现「战意 90% 然后突然暴毙」这种一眼假的假过程
- ✅ **风险系数透明化**：报名回执与个人战报直接显示成因链
  `风险系数 9.0%（事件基准 50%）/ 境界优势 ×0.72 / 战力优势 ×0.25`
- ✅ **不增加操作负担**：全程只读，玩家不需要额外操作；报名人数 < 2 时不发回合播报（避免单人事件刷屏），
  这类事件仍完整记录个人战报，可用指令回看

### 2. 死亡率模型重做：符合小说设定

**问题**：旧模型把境界优势摊在整条推荐区间上（跨度 8 级时领先 1 级只值 6.25%），
战力减免还是 1.5x/2x/3x/4x 的硬阶梯且**封顶 30%**。实测「化神中期 + 满神装满丹 + 领先一个小境界」
在高阶事件（基准 50%）里死亡率仍高达 **32.8%**——堆了一身神装几乎没用。

**改进**：`最终死亡率 = 事件基准 × 境界优势 × 战力优势`

| 因子 | 规则 | 参数（面板 / config） |
|------|------|----------------------|
| 境界优势 | 每高出推荐门槛 1 个小境界 ×0.72 | `DEATH_MINOR_DECAY` |
| 战力优势 | 连续曲线 `1 - 1/(1 + k×(战力倍率-1))`：2 倍→减半，4 倍→降到 1/4 | `COMBAT_POWER_DECAY` |
| 战力上限 | 最多减免 **90%**（旧版 30%） | `COMBAT_POWER_MAX_REDUCTION` |
| 大境界压制 | 高出事件推荐上限一个大境界完全免死（v3.9.4 规则保留） | — |
| 免死线 | 最终风险 < 1.5% 视同完全免死，不再掷骰 | `DEATH_ZERO_THRESHOLD` |

**同一场景实测对比**（高阶事件 min=19 化神初期 / max=27 合体后期 / 基准 50%）：

| 境界 | 旧模型·裸装 | 旧模型·满神装 | **新模型·裸装** | **新模型·满神装** |
|------|------------|--------------|----------------|------------------|
| 化神初期（门槛） | 50.0% | 35.0% | 50.0% | 12.5% |
| 化神中期（+1 小境界） | 46.9% | 32.8% | 36.0% | **9.0%** |
| 炼虚初期 | 40.6% | 28.4% | 18.7% | 4.7% |
| 合体初期 | 31.3% | 21.9% | 7.0% | 1.7% |
| 合体后期 | 25.0% | 17.5% | 3.6% | 免死 |
| 大乘初期 | 免死 | 免死 | 免死 | 免死 |

- ✅ 断崖收敛：`合体后期 17.5% → 大乘 0%` 收成 `3.6% → 0%`
- ✅ **「异变」死因**：每回合风险 < 5% 却仍然阵亡时自动判定为异变，从成因池抽取离谱但可信的
  死因写进播报（「被三名敌手合围，力竭被围」「为掩护道友撤退，独自殿后断敌」等），
  确保「我很安全却死了」永远有一个可见解释

### 3. AI 叙事分层（P1）

- ✅ **事实归代码、氛围归 AI**：所有人名、数值、伤亡、掉落都由
  `managers/world_event_battle.py` 按真实战况写入播报；AI 只在回合播报里追加一句氛围描写，
  提示词明确禁止出现人名与数值 —— AI 无论怎么幻觉都无法伪造战果
- ✅ **调用量反降**：回合文案按「模板 + 回合 + 伤亡档位」缓存并全群复用，与群数无关；
  同时把「开场文案在创建与开战各生成一次」的多余调用合并（落库到事件快照）。
  每轮 AI 调用从 `1 + 2N` 降到约 `1 + N`（N = 群数），而群内播报量翻了 2 倍多
- ✅ **新增面板开关**「AI 文案生成 → 启用 AI 战况叙事」（默认关闭，失败自动回退固定文案池）

### 4. BUG 修复

- 🐛 面板「世界事件配置」的 **启用战力影响死亡率**（`ENABLE_COMBAT_POWER_REDUCTION`）与
  **战力最大减免比例**（`COMBAT_POWER_MAX_REDUCTION`）此前未登记进键名映射表，
  且 `_extract_plugin_world_event_config` 要求键名存在于默认配置中才会采纳，
  导致**在后台改这两个值完全无效**（只有改 `config/world_events.json` 才生效）。已修复。
- 🐛 修复 `handlers/misc_handler.py` 帮助总览标题停留在 v3.8.1 的版本号漂移。

### 5. 数据库变更

- ✅ **v41 迁移**：`world_events` 新增 `battle_state TEXT NOT NULL DEFAULT ''`（战斗过程快照）
  与 `battle_node INTEGER NOT NULL DEFAULT 0`（已结算回合数，用于 CAS 占位防重复播报）。
  迁移幂等，`battle_state` 为空表示该事件未启用可见战斗过程，结算自动走旧版路径，
  因此升级期间正在进行中的事件也能正常结算。

### 6. 新增 / 变更指令与配置

**新增指令**

| 指令 | 说明 |
|------|------|
| `世界事件战报` | 查看最近一场世界事件的逐回合回放（个人视角，含风险系数成因） |

**已废弃参数**（v3.10.0 起不参与计算，保留键名以兼容旧配置文件）：
`death_in_range_reduction`、`death_over_level_decay`、`death_safe_level_gap`、`death_under_level_penalty`

---

## v3.9.4 (2026-09-22) - 世界事件优化 + 境界压制重构 + 小队便利化 🎁

**🎯 核心改进**

### 1. 世界事件个人奖励查询
**问题**：玩家不知道自己在世界事件中具体获得了什么
- 结算消息只显示稀有掉落汇总（所有人的物品加总）
- 玩家需要去储物戒慢慢找自己获得的物品
- 体验不佳

**改进**：
- ✅ **新增指令**：`/我的世界事件奖励` - 查看个人奖励明细
- ✅ **数据库升级**：v40 迁移，为 `event_participants` 表添加 `rewards` 字段
- ✅ **详细展示**：灵石、修为、物品掉落、丹方、称号一目了然
- ✅ **区分状态**：显示已入库和未入库物品（储物戒满的情况）
- ✅ **结算提示**：世界事件结算后提示玩家使用新指令查看个人奖励

**效果示例**：
```
🎁 世界事件个人奖励
━━━━━━━━━━━━━━━
🌟 【上古遗迹现世】
📊 结算结果：✅ 生还
━━━━━━━━━━━━━━━
💰 灵石：+25,000
✨ 修为：+80,000
━━━━━━━━━━━━━━━
📦 物品掉落：
  • 灵髓精华
  • 九转仙草
  • 太古龙骨
```

---

### 2. 境界压制系统重构 ⚔️
**问题**：原境界压制逻辑不符合修仙小说设定
- 高出推荐上限6级才完全免死（固定值，不考虑大境界）
- 所有境界统一60%衰减系数，后期小境界差距体现不足
- 不符合"高一个大境界就是碾压"的修仙设定

**改进**：
- ✅ **大境界碾压**：高出一个大境界（筑基 vs 炼气、金丹 vs 筑基等）**完全免死**
- ✅ **小境界质变**：后期境界每个小境界差距更大
  - **合体期-大乘期** (≥25级)：每小境界衰减到 **30%** 🔥
  - **化神期-炼虚期** (19-24级)：每小境界衰减到 **40%**
  - **金丹期-元婴期** (13-18级)：每小境界衰减到 **50%**
  - **炼气期-筑基期** (0-12级)：每小境界衰减到 **60%**
- ✅ **动态判断**：通过境界名称识别大境界，而非固定级数

**对比案例**（化神期事件，基础死亡率50%）：

| 玩家境界 | 原逻辑（60%衰减） | 新逻辑（40%衰减） | 优化效果 |
|---------|----------------|----------------|---------|
| 炼虚期初期（高1级） | 30% | **20%** | ⬇️ 10% |
| 炼虚期中期（高2级） | 18% | **8%** | ⬇️ 10% |
| 炼虚期后期（高3级） | 11% | **3.2%** | ⬇️ 7.8% |
| **合体期初期** | 6.5% | **0%** | ✨ 完全免死 |

**符合修仙设定**：
- ✅ 筑基期参加炼气期事件 → 不会死
- ✅ 高阶修士每个小境界都是质的飞跃
- ✅ 大境界的差距是质的飞跃，不是量的积累

---

### 3. 小队探索自动分配 🤝
**问题**：队长需要手动逐个分配物品
- 每次 `/分配物品 <编号> @某人` 逐个操作
- 物品多的时候非常繁琐
- 降低组队探索体验

**改进**：
- ✅ **新增指令**：`/自动分配` - 队长一键平均分配所有战利品
- ✅ **轮流分配算法**：按队员顺序轮流分配，公平合理
- ✅ **详细反馈**：显示每个物品分配给了谁
- ✅ **容错处理**：储物戒满或其他错误时单独列出失败物品

**使用示例**：
```
队长：/自动分配

✅ 自动分配完成！
━━━━━━━━━━━━━━━

📦 已分配物品：
  • 📦灵髓精华 x1 → 张三
  • 📦月华精粹 x2 → 李四
  • ⚔️寒霜剑 x1 → 王五
  • 🔥五品紫金丹 x3 → 张三

⚠️ 分配失败：
  • 九转仙草（李四储物戒已满）
```

---

**📝 修改文件清单**
- `managers/world_event_manager.py` - 新增个人奖励查询、境界压制重构
- `handlers/world_event_handlers.py` - 新增 `handle_rewards()` 方法
- `handlers/team_handlers.py` - 新增 `handle_auto_assign_loot()` 方法
- `data/migration.py` - 数据库迁移v40，新增 `event_participants.rewards` 字段
- `main.py` - 注册新指令：`/我的世界事件奖励`、`/自动分配`

**🔧 数据库变更**
- 版本：v39 → v40
- 新增字段：`event_participants.rewards` (TEXT) - 保存玩家个人奖励明细

---

## v3.9.3 (2026-09-16) - 饰品装备系统 + 武器属性修复 💎

**🎯 核心改进**

### 1. 饰品装备系统上线
**新增**：饰品装备栏，玩家现可装备饰品类法器
- ✅ **新增装备栏位**：武器、防具、**饰品**（新）、主修心法、功法×3
- ✅ **饰品属性计入战力**：法伤、物伤、法防、物防、精神力全部生效
- ✅ **装备/卸下指令支持**：`/装备 饰品名称`、`/卸下 饰品`
- ✅ **储物戒兼容**：卸下的饰品自动存入储物戒

**可装备饰品**（items.json）：
- 镇魂幡（圣品）- 攻击100 + 防御50
- 戮仙剑阵（帝品）- 攻击800 + 防御200

**数据库变更**：
- 新增 `players.accessory` 字段（TEXT，默认空字符串）
- 迁移版本：v37 → v38

---

### 2. 武器属性修复
**问题**：多个武器配置缺少完整属性，导致装备后无加成
- 青锋剑、烈阳刀、寒霜剑、妖精之弓、弑神枪只有 `attack` 字段
- 缺少法伤、物伤、物防、精神力等关键属性

**修复**：
- ✅ 补全所有武器的5项属性：`magic_damage`、`physical_damage`、`physical_defense`、`mental_power`
- ✅ 调整弑神枪属性以匹配999万天价（法伤900、物伤1100、物防400、精神950）

**修复的武器**：
| 武器名 | 品级 | 价格 | 新增属性 |
|-------|------|------|---------|
| 青锋剑 | 凡品 | 800 | 法伤5、物伤10、物防3、精神5 |
| 烈阳刀 | 珍品 | 4000 | 法伤30、物伤40、物防15、精神25 |
| 寒霜剑 | 圣品 | 60万 | 法伤150、物伤130、物防60、精神120 |
| 妖精之弓 | 圣品 | 55万 | 法伤180、物伤240、物防80、精神160 |
| 弑神枪 | 帝品 | 999万 | 法伤900、物伤1100、物防400、精神950 |

---

**📝 修改文件清单**
- `models.py` - Player类新增 `accessory` 字段
- `core/equipment_manager.py` - 饰品装备/卸下/属性计算逻辑
- `data/migration.py` - 数据库迁移v38，新增 `accessory` 列
- `config/items.json` - 修复5个武器的属性配置

---

## v3.9.2 (2026-09-15) - 死亡系统简化 + 战力影响死亡率 ⚡

**🎯 核心改进**

### 1. 死亡系统简化
**问题**：原死亡机制过于复杂且惩罚过重
- 死亡后每小时流失1%修为
- 到时间还需手动输入命令才能复活
- 玩家体验差，容易忘记复活

**改进**：
- ✅ **死亡时立即扣除可配置的修为百分比**（默认5%），之后不再流失
- ✅ **到时间自动复活**，无需手动输入命令
- ✅ 系统每5分钟自动检查元神玩家，到时间立即复活

**配置项**（`config/death_config.json`）：
```json
{
  "soul_revival_hours": 24,      // 复活等待时间（小时），-1=需还魂丹
  "soul_exp_loss_rate": 0.05,    // 死亡扣除修为比例（5%）
  "rebirth_exp_keep_rate": 0.7   // 劫后重生保留修为比例（70%）
}
```

**移除配置**：
- ❌ `soul_exp_decay_per_hour`（每小时流失比例）- 已删除

---

### 2. 战力影响死亡率
**问题**：装备和丹药对世界事件死亡率没有任何影响
- 同境界裸奔和神装+buff死亡率完全一样
- 玩家磕药、提升装备没有实际收益
- 降低参战积极性

**改进**：
- ✅ **装备属性计入战力**：物攻、法攻、物防、法防、精神力
- ✅ **丹药buff计入战力**：临时增益效果实时生效
- ✅ **战力越高死亡率越低**：最多减免30%死亡率

**战力减免档位**：

| 战力提升比例 | 死亡率减免 | 说明 |
|------------|----------|------|
| 1.0x - 1.5x | 0% - 7.5% | 裸奔/基础装备 |
| 2.0x | 15% | 优秀装备 |
| 3.0x | 25% | 神装+buff |
| 4.0x+ | **30%** (上限) | 满配+丹药 |

**实战效果**（化身初期28级打高阶事件为例）：

| 装备情况 | 基础死亡率 | 战力减免 | 最终死亡率 |
|---------|----------|---------|----------|
| 裸奔 | 15% | 0% | **15%** |
| 普通装备 | 15% | 7.5% | **13.9%** |
| 优秀装备 | 15% | 15% | **12.8%** |
| 神装+buff | 15% | 25% | **11.3%** |
| 满配+吃药 | 15% | 30% | **10.5%** |

**配置项**（`config/world_events.json`）：
```json
{
  "enable_combat_power_reduction": true,  // 是否启用战力影响
  "combat_power_max_reduction": 0.30      // 最大减免30%
}
```

---

**📝 修改文件清单**
- `core/death_manager.py` - 简化死亡逻辑，新增自动复活
- `managers/world_event_manager.py` - 加入战力影响死亡率计算
- `main.py` - 新增自动复活定时任务，注入装备管理器
- `config/death_config.json` - 移除每小时流失配置
- `config/world_events.json` - 新增战力影响配置
- `.claude/skills/xiuxian-dev-guide/SKILL.md` - 更新开发文档

---

## v3.9.1 (2026-09-15) - 死亡机制修复：世界事件境界压制 / 回生丹 / 救援复活 🐛

**🐛 事故复盘（触发本次修复）**

| 环节 | 问题 |
|------|------|
| 案例 | 22 级炼虚期修士在**低阶事件「妖兽围城」**（推荐境界 1-12、基础死亡率 15%）中阵亡进入元神 |
| 根因 | `WorldEventManager._calc_death_rate()` 只对「低于门槛」的玩家加死亡率，**从不因境界高而下调**，也不看战力/装备/丹药 |
| 加剧 | 报名只校验 `min_level`、不校验 `max_level`，高境界玩家可参加低阶事件，并被同一裸概率骰死 |

**🛡️ 改造一：世界事件死亡率「境界压制」模型**

`death_rate = 基础死亡率 × 境界系数`，境界越高越难死，压制到一定程度后**完全免死**：

| 参数（面板「世界事件配置」） | 默认 | 说明 |
|------|------|------|
| `death_in_range_reduction` | 0.5 | 推荐区间内：死亡率从门槛处的 100% 线性衰减到上限处的 50% |
| `death_over_level_decay` | 0.6 | 高出推荐上限后，每高 1 级死亡率 ×0.6 |
| `death_safe_level_gap` | 6 | 高出推荐上限 6 级后**死亡率归 0（不掷骰子，绝对免死）**，填 0 关闭 |
| `death_under_level_penalty` | 0.2 | 低于事件最低门槛时额外 +20% 死亡率 |

改造后各档实际死亡率（实测 300 次/样本，误差 <2%）：

| 难度 | 门槛处 | 推荐上限处 | 免死阈值 |
|------|--------|-----------|----------|
| 低阶（1-12） | 15% / 20% | 7.5% / 10% | 18 级起 0% |
| 中阶（13-18） | 35% | 17.5% | 24 级起 0% |
| 高阶（19-27） | 50% | 25% | 33 级起 0% |
| 史诗（28-36） | 70% | 35% | 42 级起 0%（当前境界上限内不触发） |

> 本次案例：22 级打低阶事件高出上限 10 级 → **0%，从机制上杜绝"炼虚期死于妖兽围城"**。

**🛡️ 改造二：回生丹可抵消世界事件阵亡**
- 持有回生丹的玩家在世界事件中阵亡时，优先被丹药抵消（属性减半但活下来），并**按生还者拿全额奖励**
- 与突破走火入魔共用 `PillManager.handle_resurrection()`，两处死亡语义一致
- 结算广播新增 `🛡️ 回生丹显灵·免于一死（全额奖励）`

**🔧 改造三：修复 4 个关联 BUG**

| # | 位置 | 问题 | 影响 |
|---|------|------|------|
| 1 | `main.py` 宗门救援 / 师父救援 | 复活时写了一个**不存在的字段** `soul_state_start_time`（正确字段是 `soul_death_time`），也未走复活结算 | 被救者修为永久停在被砍半的 **50%**，`soul_death_time` / `soul_exp_before_death` 残留。现统一改调 `RevivalHandler.revive_soul(reason="rescue")`，**无损复活** |
| 2 | `main.py` 师父救援 | 调用了不存在的方法 `mentorship_mgr.rescue_apprentice()`（实际名为 `rescue_disciple()`） | 该指令**直接抛 AttributeError**，师父救援从未可用 |
| 3 | `utils/ai_generator.py` | `generate_event_summary()` 只接收 4 个参数，而调用方传了 5 个（多传 `rewards_summary`） | AI 战况总结**每次都抛错降级**为固定模板。现已接收并把稀有掉落写进提示词 |
| 4 | `handlers/revival_handler.py` | 复活方式提示仍写「请求宗门/师父救援（后续开放）」 | 功能已上线，文案同步为「请求宗门（宗主/长老）或师父救援」 |

**⚙️ 配置面板新增**（世界事件配置分组）
- 推荐区间内死亡率衰减比例 / 高出推荐上限的每级衰减系数 / 绝对免死境界差 / 低于门槛的额外死亡率

**📦 数据库**
- **无表结构变更**（沿用 v36），无需迁移；事件模板与配置均在 `config/world_events.json`

**🧪 验证**
- 世界事件结算 E2E（900 次事件实测 + 单点死亡率验算）：低阶事件 18 级以上零阵亡、回生丹 300 次全部免死
- 复活的字段清理、修为无损、自然复活 5% 损失均已覆盖测试

## v3.9.0 (2026-09-13) - 系统优化与扩展（批次6）

### 🌾 灵田系统改造：高级灵草种植

**现状问题**：灵田只能种植5种低级灵草（1-2星材料），高级丹药（还魂丹5星、悟道丹4星）需要的材料无法自给，玩家只能通过世界事件获取，缺乏经营线。

**改造目标**：让灵田成为"慢速稳定产出"的高级材料来源，与世界事件形成互补：
- **世界事件**：高风险快产出（主要来源）
- **灵田高级种植**：低风险慢产出（24-72h成熟，稳定供给）
- **百宝阁**：花钱买时间（随机货架）

**新增灵草**：

| 星级 | 灵草名称 | 成熟时间 | 灵田等级 | 种子成本 | 产出材料 | 用途 |
|------|---------|---------|---------|---------|---------|------|
| 3星 | 悟道草 | 24小时 | Lv.3 | 5,000灵石 | 1个 | 4星丹药材料 |
| 3星 | 护心草 | 24小时 | Lv.3 | 5,000灵石 | 1个 | 4星丹药材料 |
| 4星 | 九转仙草 | 48小时 | Lv.4 | 15,000灵石 | 1个 | 5星丹药材料（还魂丹） |
| 4星 | 太古龙骨 | 48小时 | Lv.4 | 15,000灵石 | 1个 | 5星丹药材料（还魂丹） |
| 5星 | 灵髓精华 | 72小时 | Lv.5 | 30,000灵石 | 2个 | 顶级丹药材料 |
| 5星 | 月华精粹 | 72小时 | Lv.5 | 30,000灵石 | 2个 | 顶级丹药材料 |

**种植改动**：
- 种植高级灵草需支付种子成本（灵石）
- 检查灵田等级是否满足要求
- 收获时获得：修为 + 灵石 + 材料物品（存入储物戒）
- 基础灵草（1-2星）保持免费种植

### 🏛️ 宗门救援复活

让宗门成为复活互助的情感纽带，增加社交玩法。

**指令**：`宗门救援 <@某人>`

**功能特点**：
- **权限**：宗主/长老可发起
- **代价**：消耗500宗门贡献度
- **效果**：被救者立即复活，保留100%修为（无损失）
- **限制**：
  - 每人每日1次
  - 被救者必须在该宗门
  - 宗门建设度需>=5000
  - 被救者必须处于元神状态

**使用场景**：
```
宗门成员阵亡进入元神状态
→ 宗主/长老发送「宗门救援 @某人」
→ 消耗500贡献度
→ 被救者立即复活，修为无损
→ 群内广播救援成功消息
```

### 👨‍🏫 师徒救援复活

师父牺牲自己修为救徒弟，增强师徒情感羁绊。

**指令**：`救援徒弟 <@某人>`

**功能特点**：
- **权限**：仅师父可发起
- **代价**：师父损失5%当前修为
- **效果**：徒弟立即复活，保留100%修为（无损失）
- **限制**：
  - 每个徒弟每日1次
  - 必须是活跃的师徒关系
  - 师父修为需>=10%（保底）
  - 徒弟必须处于元神状态

**使用场景**：
```
徒弟阵亡进入元神状态
→ 师父发送「救援徒弟 @某人」
→ 师父损失5%修为
→ 徒弟立即复活，修为无损
→ 显示「师恩如山，永志不忘」
```

### 📊 复活路线对比

| 复活方式 | 等待时间 | 修为损失 | 代价 | 限制 |
|---------|---------|---------|------|------|
| 自然复活 | 24小时 | 5% | 无 | 需等待 |
| 还魂丹 | 立即 | 0% | 消耗还魂丹 | 需炼制/委托/事件 |
| 宗门救援 | 立即 | 0% | 500贡献度 | 每日1次，建设度5000+ |
| 师徒救援 | 立即 | 0% | 师父损失5%修为 | 每日1次 |

### 🔧 技术实现

**新增方法**：
- `managers/sect_manager.py::rescue_member()` - 宗门救援
- `managers/mentorship_manager.py::rescue_disciple()` - 师徒救援

**新增表结构**：
- `sect_rescue_log` - 宗门救援冷却记录
- `mentorship_rescue_log` - 师徒救援冷却记录

**新增指令**：
- `宗门救援` - 宗门救援复活
- `救援徒弟` - 师徒救援复活

### 📝 其他改进

- 灵田收获时将材料存入储物戒（而非仅显示）
- 高级灵草种植失败时提示具体原因（等级不足/灵石不足）
- 批量种植时检查种子成本总额

---

## v3.8.1 (2026-09-12) - 死亡惩罚/AI 配置接入 AstrBot 面板 + 批次5 修复

### ⚙️ 新功能：死亡惩罚与世界事件参数可在 AstrBot 后台配置

- 插件配置面板新增分组（保存后重载插件生效）：
  - **死亡与复活配置**：`SOUL_REVIVAL_HOURS`（元神自然复活小时数）、`SOUL_EXP_LOSS_RATE`
    （自然复活永久损失修为比例）、`REBIRTH_EXP_KEEP_RATE`（劫后重生保留修为比例）、
    `SOUL_EXP_DECAY_PER_HOUR`（元神每小时修为流失比例）
  - **世界事件配置**：`AUTO_GENERATE`、`AUTO_INTERVAL_MINUTES`、`SIGNUP_DURATION_SECONDS`、
    `MIN_PARTICIPANTS`、`MAX_PARTICIPANTS`、`DEATH_PENALTY_REWARD_RATE`（阵亡者奖励保留比例）、
    `BROADCAST_GROUPS`、`ADMIN_USERS`
  - **AI 文案生成**：`ENABLE_AI_INTRO`、`ENABLE_AI_SUMMARY`、`AI_API_KEY`、`AI_BASE_URL`、
    `AI_MODEL`、`FIXED_FALLBACK`
- 配置优先级统一为：**插件配置面板 > config/*.json > 内置默认值**

### 🐛 修复：配置读取与批次5 功能缺陷

| 问题 | 说明 |
|------|------|
| 死亡配置面板不生效 | 新增 `core/death_manager.resolve_death_config()` 统一解析（默认值 → `death_config.json` → 插件面板），突破、世界事件阵亡、复活系统均改为走该入口 |
| `RevivalHandler` 读取配置报错 | 原实现调用不存在的 `config_manager.get(...)`，异常被吞掉导致**一直使用默认值**（配置文件与面板都被忽略），现已修复 |
| 管理员权限无法生效 | `is_admin()` / 群号白名单同样调用了不存在的 `config_manager.get(...)`，现改为读取合并后的配置（面板 `ADMIN_USERS` / `BROADCAST_GROUPS`，兼容旧的顶层 `world_event_admin_users`） |
| `查看世界事件` 报错 | `DataBase` 缺少 `list_world_events()`，已补齐；同时补充 `get_world_event()` / `cancel_world_event()`（`结束世界事件` 依赖） |
| 管理员创建的事件没广播 | `创建世界事件` 成功后现在会立即把事件广播到目标群（含 AI 开场文案），此前只有一句「实际广播由定时任务处理」的空实现 |
| 私聊判断可能报错 | `event.message_obj.is_private` 改为兼容实现（优先用 `get_group_id()` 判断，失败再回退到 `message_obj.is_private`） |
| AI 文案从未生效 | `utils/ai_generator.py` 此前只是独立文件、没有任何调用点；现已接入事件开场与结算总结，并改为读取插件面板 `AI_GENERATOR` 分组（同时改用 AstrBot logger） |

### 🤖 AI 文案接入细节

- 事件开战 / 自动生成广播：调用 `generate_event_intro()`，失败或未启用时回退固定模板
- 事件结算广播：调用 `generate_event_summary()`，生成含阵亡原因的战况总结
- AI 调用通过 `asyncio.to_thread` 执行，避免阻塞事件循环；`requests` 已加入 `requirements.txt`
- 兼容 OpenAI 格式的任意服务（OpenAI / DeepSeek / 硅基流动 / OpenRouter 等）


## v3.8.0 (2026-09-11) - 管理员功能与AI集成（批次5）

### 🛠️ 管理员指令（仅私聊bot）

- **创建世界事件**：`创建世界事件 <难度> <群号> [奖励倍率]`
  - 管理员手动创建并广播世界事件
  - 难度支持：低阶/中阶/高阶/史诗
  - 可自定义奖励倍率（默认2.0x）
  - 示例：`创建世界事件 高阶 123456789 3.0`
- **查看模板**：`世界事件模板`
  - 列出所有可用事件模板（按难度分类）
- **监控事件**：`查看世界事件`
  - 查看所有活动事件（报名中+进行中）
  - 显示事件ID、难度、状态、参与人数
- **强制结束**：`结束世界事件 <事件ID>`
  - 管理员强制结束某个事件

### 🤖 AI文案生成（可选）

- **开场描述**：事件创建时生成修仙风格开场文案（100字左右）
- **战斗总结**：事件结束时生成含阵亡/幸存者故事的总结（150-200字）
- **支持平台**：OpenAI（GPT-4/3.5）、Anthropic（Claude）
- **降级保护**：AI调用失败自动使用固定模板
- **配置选项**：
  - `enable_ai_intro`：启用AI开场描述
  - `enable_ai_summary`：启用AI战斗总结
  - `ai_provider`：openai / anthropic
  - `ai_api_key`：API密钥
  - `ai_model`：模型名称（gpt-4 / claude-3-opus-20240229）
  - `ai_base_url`：API地址（可选，支持中转）
  - `fixed_fallback`：AI失败时是否降级为固定模板

### 📝 配置文件扩展

- `config/world_events.json` 新增字段：
  - `admin_users`：管理员QQ号列表
  - `broadcast_groups`：允许创建事件的群号白名单（空=所有白名单群）
  - `ai_config`：AI生成器配置块

### 🔐 权限控制

- 所有管理员指令仅限私聊使用
- 管理员权限从配置文件读取（`admin_users`）
- 群号白名单限制（`broadcast_groups`）
- 非管理员调用时提示无权限

### 🏗️ 架构优化

- 新增 `utils/ai_generator.py`：AI文案生成器（可插拔设计）
- 新增 `handlers/admin_handlers.py`：管理员指令处理器
- 扩展 `managers/world_event_manager.py`：5个管理员专用方法
  - `is_admin()`：权限验证
  - `create_admin_event()`：创建事件
  - `list_event_templates()`：列出模板
  - `get_active_events()`：监控事件
  - `force_end_event()`：强制结束

### 💡 使用说明

AI功能默认关闭，需手动配置：
```json
{
  "ai_config": {
    "enable_ai_intro": true,
    "enable_ai_summary": true,
    "ai_provider": "openai",
    "ai_api_key": "sk-your-key-here",
    "ai_model": "gpt-4"
  }
}
```

---

## v3.7.0 (2026-09-11) - 世界事件系统（替代历练，批次4）

### 🌍 新功能：世界事件

- 新增指令：`世界事件` / `加入世界事件` / `退出世界事件` / `世界事件战绩`
- **自动生成与广播**：默认每 2-4 小时自动开启一场（`auto_interval_minutes`），
  广播目标优先取 `broadcast_groups`，留空则使用插件白名单群
- **状态机自动推进**（每 30 秒 tick 一次，无玩家操作）：
  报名（默认 300 秒）→ 报名截止自动开战 → 事件时长结束自动结算 → 广播结果
- **四档难度**：低阶（推荐 1-12）/ 中阶（13-18）/ 高阶（19-27）/ 史诗（28-36），
  基础死亡率 15% / 20% / 35% / 50% / 70%
- **风险随境界浮动**：境界低于推荐区间中段额外 +10% 死亡率，低于下限 +20%
- **奖励结算**：幸存者全额奖励；阵亡者保留 `death_penalty_reward_rate`（默认 20%）；
  储物戒空间不足时会在结算文案中提示丢失的物品
- **阵亡触发死亡机制**（批次1 规则）：
  练气-筑基「劫后重生」（境界-1、保留 70% 修为，仅一次），再死彻底陨落；
  金丹期以上进入【元神状态】，可自然复活或使用还魂丹
- **稀有掉落**：九转仙草 / 太古龙骨 / 灵髓精华 / 月华精粹（还魂丹主材）、
  魔核 / 魔晶 / 天魔精华 / 混沌石 / 仙灵草 / 仙器碎片（新增 9 种材料已登记 items.json）
- **专属装备**：魔修战甲 / 嗜血魔刃（中阶）、上古神兵 / 仙家法宝（高阶）、传说神器（史诗），
  由 `WorldEventManager.get_equipment_config` 动态生成属性，可正常穿戴
- **丹方与称号**：高阶/史诗掉落还魂丹（101）、悟道丹（104）丹方；史诗事件 5% 概率授予「炼丹师」称号
- **体系联动**：生还者推进对应难度的悬赏进度；师徒传道奖励恢复（徒弟参战收益的 8% 归师父）
- **待扩展接口**：`create_event(..., admin_created=True, reward_multiplier=...)` 已支持自定义难度与奖励倍率，供批次5 管理员指令使用

### 🗑️ 移除历练系统

- 下线指令 `开始历练` / `完成历练` / `历练状态` / `历练信息`，帮助主题 `历练帮助` → `世界事件帮助`
- `managers/adventure_manager.py`、`handlers/adventure_handlers.py`、`config/adventure_config.json`
  改名为 `*.deprecated` 保留（回滚只需去掉后缀并恢复引用），不再被插件导入
- 悬赏模板的 `adventure_*` 标签替换为 `world_event_low/mid/high/epic`，避免悬赏无法完成
- 升级时自动清理 `user_cd` 中遗留的「历练中」忙碌状态，避免玩家卡死

### 🔧 重构：死亡结算统一

- 新增 `core/death_manager.py`（`DeathManager` / `DeathOutcome`），把批次1 的分级死亡规则抽为单一实现
- `BreakthroughManager` 与世界事件共用同一套死亡逻辑，文案与数值保持一致，避免规则漂移

### 🗄️ 数据库变更（v36，含启动自检）

| 表 / 字段 | 说明 |
|-----------|------|
| `world_events` | 世界事件主体：群号、事件快照、状态机时间（报名截止 / 开战 / 结束 / 结算） |
| `event_participants` | 报名名单与结算结果（生还 / 阵亡 / 陨落），带索引 |
| `sect_daily_tasks.completed_world_event` | 宗门每日任务「参与世界事件」列（原 `completed_adventure` 保留兼容） |
| `user_cd` 清理 | 删除 `type = 2`（历练）的遗留忙碌状态 |

### ⚙️ 新增配置：`config/world_events.json`

| 配置项 | 默认值 | 说明 |
|--------|--------|------|
| `auto_generate` | true | 是否自动生成世界事件 |
| `auto_interval_minutes` | [120, 240] | 自动生成间隔（分钟，区间内随机） |
| `signup_duration_seconds` | 300 | 报名时长（秒） |
| `min_participants` / `max_participants` | 1 / 10 | 开战下限 / 报名上限 |
| `death_penalty_reward_rate` | 0.2 | 阵亡者奖励保留比例 |
| `auto_tier_weights` | 40/30/20/10 | 自动生成时低/中/高/史诗难度权重 |
| `broadcast_groups` | [] | 事件目标群（留空则使用白名单群） |
| `admin_users` | [] | 可手动创建事件的管理员（批次5 使用） |

### 🧩 其他

- 元神状态限制同步：允许 `世界事件` / `世界事件战绩`（只读），禁止 `加入世界事件`
- 忙碌（闭关）状态下允许查看与报名世界事件
- 材料来源文案、储物戒/商店提示、`ItemRegistry` 物品索引、悬浮提示统一改为「世界事件」

## v3.6.0 (2026-09-11) - 炼丹师职业与委托炼丹（批次3）

### ✨ 新功能

#### 炼丹师称号

- 新增指令 `成为炼丹师`：金丹期（境界 13）+ 成功炼制 10 次丹药 + 10000 灵石
- 获得称号后：
  - 炼丹成功率 **+15%**（与境界加成叠加，上限 95%）
  - 10% 概率额外产出一枚丹药（沿用批次2 的 `alchemist_extra_pill_chance`）
  - **自动解锁稀有配方**（`alchemist_rare_recipes`，默认还魂丹 101）
- 新增炼丹统计 `alchemy_stats`：每次炼丹（成功 / 失败）都会累计
  - `AlchemyManager.craft_pill` 结算时调用 `DataBase.record_alchemy_attempt`
  - `AlchemyManager.check_alchemist_qualification` 依据成功次数判定资格
- 新增指令 `炼丹师信息`：炼丹统计、称号状态、进行中的委托、炼丹师 Top5 排行

#### 委托炼丹（玩家间炼丹服务交易）

| 指令 | 说明 |
|------|------|
| 委托炼丹 <配方ID> [数量] [手续费] [@炼丹师] | 发布委托（材料 + 手续费托管锁定） |
| 委托列表 | 查看公开委托板（按手续费从高到低） |
| 我的委托 | 查看自己发布 / 接单的委托与状态 |
| 接取委托 <编号> | 炼丹师接单（领取托管材料） |
| 完成委托 <编号> | 炼丹师交付成品并领取手续费 |
| 取消委托 <编号> | 委托人撤回未接单的委托（全额返还） |
| 放弃委托 <编号> | 炼丹师放弃已接单的委托（退回委托人） |

- **托管机制**：发布时材料（含配方中的灵石消耗）与手续费从委托人身上扣除并锁定，
  完成前不落入任何一方；取消 / 放弃时原样返还
- **接单转交材料**：接单前校验炼丹师资格、境界、稀有配方学习状态、储物戒剩余格数
- **交付成品**：从炼丹师丹药背包扣除成品交付委托人，同时支付托管手续费
- **指定炼丹师**：`@某人` 后仅该炼丹师可接单；未指定则进入公开委托板
- **防并发**：`claim_commission` 采用条件 UPDATE，避免两位炼丹师抢同一单
- **上限保护**：未接单委托 `commission_max_pending`(5) / 进行中委托 `commission_max_active`(3)
  / 单笔数量 `commission_max_quantity`(99) / 手续费上限 `commission_max_fee`(1000000)
- **储物戒空间保护**：退回材料前先校验委托人剩余格数，空间不足时保持委托不变，避免材料丢失

### 📦 数据库变更

- 新增 v35 迁移（启动自检幂等补齐，`repair_commission_tables`）：
  - `alchemy_stats`：`user_id / total_attempts / success_count / last_attempt_time`
  - `alchemy_commissions`：`commission_id / client_id / alchemist_id / recipe_id / quantity /
    fee / status / materials_json / create_time / accept_time / complete_time`（含 3 个索引）
- `delete_player_cascade` 同步清理炼丹统计与委托记录

### 🧩 新增文件

| 文件 | 说明 |
|------|------|
| `managers/commission_manager.py` | 委托炼丹管理器（发布 / 查询 / 接单 / 交付 / 取消 / 放弃） |
| `handlers/commission_handlers.py` | 委托炼丹指令处理器（含参数解析与 @炼丹师 解析） |

### 🔧 其他调整

- `managers/alchemy_manager.py`：新增 `check_alchemist_qualification` / `grant_alchemist_title` /
  `get_alchemist_config` / `get_rare_recipes` / `get_alchemist_requirement_text`
- `data/data_manager.py`：新增炼丹统计与委托记录的读写方法
- `handlers/utils.py`：元神状态放行 `委托列表` / `我的委托` / `炼丹师信息` / `取消委托`，
  禁止 `成为炼丹师` / `委托炼丹` / `接取委托` / `完成委托` / `放弃委托`
- `炼丹帮助` 新增「炼丹师职业」「委托炼丹」章节；`修仙帮助` 同步更新
- 修复：`data/default_configs.py` 中 `DEFAULT_DEATH_CONFIG` 未定义导致的模块导入报错（批次1 遗留，会导致插件无法加载）

## v3.5.9 (2026-09-11) - 炼丹系统升级（丹药星级 / 稀有配方 / 还魂丹）

### ✨ 新功能

#### 丹药 1-5 星分级

- `config/alchemy_recipes.json` 新增 `star` 字段：丹药分 1-5 星
- **商店只售 1-2 星丹药**：3 星及以上丹药在丹阁 / 百宝阁均不上架
  （过滤逻辑见 `core/shop_manager.py:is_high_star_pill_item`，上限由
  `config/alchemy_config.json:max_shop_pill_star` 控制）
- 新增高阶丹药：
  - **还魂丹（5星）**：元神状态立即完美复活，修为无损
  - **悟道丹（4星）**：功法经验+100%（修炼速度+100%），持续 6 小时
  - **护道丹（4星）**：PVP 死亡不掉落装备，持续 24 小时
  - **狂暴丹（3星）**：暴击率+50%、伤害+50%，持续 1 小时
  - **金刚丹（3星）**：防御+100%，持续 1 小时
  - 「废丹」：炼制失败的副产物，可卖给 NPC 回收少量灵石
- `丹药配方` 现在展示星级、需求境界、材料、学习状态与**当前实际成功率**
- 新增指令 `配方详情 <配方ID>`：查看星级、材料持有量、是否需学习、商店是否出售

#### 还魂丹完美复活（对接批次1 元神状态）

- 新增指令 `使用还魂丹`（`服用丹药 还魂丹` 同效）：元神状态立即复活且修为无损
- 还魂丹为 `manual_only` 丹药，`一键服用` / 普通批量服用会跳过，避免误消耗
- 复活结算复用 `RevivalHandler.revive_by_pill`（无修为损失）
- 补充放行：元神状态下允许查看 `丹药配方` / `配方详情` / `材料查询` 等只读指令

#### 稀有配方学习

- 5 星与标记 `learnable: true` 的配方必须先学习，未学习时炼制会被拦截并给出获取途径
- 新增数据库方法：`has_learned_recipe` / `learn_recipe` / `forget_recipe` / `get_learned_recipes`
- 新增称号数据库方法：`has_title` / `grant_title` / `revoke_title` / `get_titles`（炼丹师，批次3 使用）

### ⚗️ 规则调整

- 成功率 = 基础成功率 + 境界加成 + 炼丹师称号加成，上限 95%
  - 境界加成：金丹期(13+) +10%、化神期(19+) +20%、合体期(25+) +30%（旧版为每高一级 +2%）
  - 炼丹师称号：+15%（批次3 委托炼丹系统开放）
  - 炼丹师炼制成功时有 10% 概率额外产出一枚
- 炼制失败：返还 **50%** 材料（含灵石），5% 概率产出「废丹」
- 修复炼丹材料消耗后可能被回写覆盖的问题（结算前重新读取玩家数据，避免脏写）

### 📦 数据库变更

- 新增 v34 迁移，创建 `learned_recipes`（已学习丹方）与 `player_titles`（玩家称号）两张表
- 启动自检会幂等补齐上述表结构；删除角色时一并清理对应数据

### 🧪 新增材料（3-5 星丹药主材）

- 九转仙草、太古龙骨、灵髓精华、月华精粹、悟道草、菩提叶、护心草、神木精华、嗜血草、妖兽内丹、金刚石
- 正式产出途径为世界事件（批次4）；试炼期已在百宝阁限量临时上架

### 🔧 帮助与引导

- `修仙帮助` 更新【炼丹系统】板块（星级、配方详情、成功率、失败规则、还魂丹）
- `炼丹帮助` 重写：丹药星级表、成功率计算、失败规则、稀有配方学习
- `死亡帮助` 更新：还魂丹已开放，并补充 `使用还魂丹` 指令说明
- README 更新炼丹系统章节与配置说明

### ⚙️ 新增配置文件

- `config/alchemy_config.json`：炼丹核心参数（境界加成 / 炼丹师加成 / 返还比例 /
  废丹概率 / 成功率上限 / 商店最高可售星级）
- `config/alchemy_recipes.json`：配方与**丹药星级**（含 101-105 号高阶配方）

## v3.5.8 (2026-09-11) - 死亡机制重构（元神状态 / 劫后重生）

### ✨ 新功能

#### 境界分级死亡机制（仅突破失败触发）

- **练气期-筑基期（境界 0-12）**：首次死亡触发「劫后重生」
  - 境界倒退 1 级（已是最低境界则保留）
  - 保留 70% 修为（可配置 `rebirth_exp_keep_rate`）
  - 清空当前状态，可继续修炼
  - **第二次死亡彻底陨落**，角色数据被清除
- **金丹期及以上（境界 13+）**：死亡不再删号，进入【元神状态】
  - 修为锁定为死亡前的 50%
  - 境界保留，肉身崩解、元神不灭
- **PVP（决斗/切磋）、Boss 战失败不会触发真正死亡**，维持原有惩罚机制
- 回生丹效果优先级不变（先判定回生丹，再判定分级死亡）

#### 元神状态

- 新增 `models.Player` 字段：`is_soul_state` / `soul_death_time` /
  `soul_exp_before_death` / `used_rebirth`
- 元神期间每小时流失 1% 修为（可配置 `soul_exp_decay_per_hour`）
- 新增指令 `元神状态`：查看已流逝时间、修为流失比例、自然复活倒计时、复活方式
- 新增指令 `自然复活`：等待期满（默认 24 小时）后复活，**永久损失 5% 修为**
- 超时未复活（默认 24 小时的 4 倍）时修为归零并强制复活
- 玩家每次操作前自动检测超时状态，避免长期卡在元神状态

#### 元神状态限制

- 禁止：闭关/出关、突破、历练、秘境探索、决斗/切磋、Boss 挑战、传承挑战、
  炼丹、种植、洞天/灵眼收取、宗门事务、悬赏、组队、贷款等主动行为
- 允许：`我的信息`、`签到`、`银行`、背包/商店/排行榜等只读指令，
  以及 `元神状态`、`自然复活`、`使用还魂丹`
- 拦截逻辑统一在 `player_required` 装饰器（`handlers/utils.py`），
  战斗 / 历练 / 秘境等未使用装饰器的入口单独补充检查

### 🔧 帮助与引导

- 新增 `死亡帮助` 主题（`handlers/help_handler.py`，别名：元神帮助、复活帮助等）
- `修仙帮助` 新增【死亡 & 复活】板块，并列出 `死亡帮助`
- `突破信息`、创建角色提示、`我的信息` 同步展示新的死亡规则与元神状态

### 📦 数据库变更

- 新增 **v33 迁移**：`players` 表补充 `is_soul_state`（默认 0）、
  `soul_death_time`（默认 0）、`soul_exp_before_death`（默认 0）、
  `used_rebirth`（默认 0），全部带默认值，向后兼容老玩家数据
- 启动自检新增 `repair_soul_state_columns`，即使版本号已最新也会校验字段

### ⚙️ 新增配置文件：`config/death_config.json`

| 配置项 | 默认值 | 说明 |
|--------|--------|------|
| `soul_revival_hours` | 24 | 元神状态自然复活所需小时数 |
| `soul_exp_loss_rate` | 0.05 | 自然复活永久损失的修为比例 |
| `rebirth_exp_keep_rate` | 0.7 | 「劫后重生」保留的修为比例 |
| `soul_exp_decay_per_hour` | 0.01 | 元神状态每小时流失的修为比例 |

### 🧩 新增文件

- `handlers/revival_handler.py`：复活系统处理器（状态查询 / 自然复活 /
  还魂丹复活接口 `revive_by_pill` / 超时强制复活）
- `utils/death_config.py`：死亡配置统一解析器（扁平/嵌套写法、类型转换、越界钳制）
- `config/death_config.json`：死亡系统配置

### 🔧 代码审查后的健壮性修复

- **配置解析统一**：原先 `ConfigManager` / `BreakthroughManager` / `RevivalHandler`
  各写一套读取逻辑，且「嵌套 `death_config` 为空字典」会吞掉外层扁平键。
  现统一走 `utils/death_config.resolve_death_config()`：
  内置默认值 → 插件配置（嵌套/扁平均可，嵌套优先）→ `config/death_config.json`（最高优先级），
  并做类型转换（`"24"` → `24.0`）与越界钳制（比例为 0~1、复活小时数为正数）
- **字段类型统一**：`Player.__post_init__` 归一化布尔字段（兼容数据库 `0/1`、
  历史空值与字符串），`data_manager` 统一使用 `models.as_flag()` / `as_int()` 写库，
  消除 `int(x or 0)` 与 `bool(x)` 混用
- **自动复活后刷新对象**：`player_required` 中检测到超时自动复活后，重新从数据库
  加载玩家对象再判断元神状态，避免继续使用过期数据
- **忙碌白名单补充**：`元神状态` / `自然复活` 加入忙碌状态白名单，
  闭关/历练等状态下也能查询与复活

## v3.5.7 (2026-09-11) - 商店境界分层与主动刷新

### ✨ 新功能

#### 商店货架按境界分层

- 丹阁 / 器阁 / 百宝阁不再共用一套全局货架，改为**按玩家境界分层**：
  只上架玩家当前修为用得上的品质档位（凡品 → 灵品 → 地品 → 天品 → 皇品 → 帝品 → 道品）
- **同境界玩家共享同一个货架**（如元婴期玩家共享「皇品」货架），
  货架 ID 形如 `pill_pavilion:t4`，谁刷新大家一起换货
- 每次生成固定 **1~2 件契合槽**（对应玩家当前境界档位，库存 1，先到先得），
  其余为次一档 / 次二档的常用品（库存 2~3），避免低阶凡品刷屏占满货架
- 物品品质档位映射：珍品≈灵品、圣品≈皇品、神品≈仙品；
  缺少 `required_level_index` 字段的物品（`items.json`）按品质兜底推导境界

#### 消耗型主动刷新

- 新增指令 `刷新商店 <丹阁|器阁|百宝阁>`，别名 `刷新坊市` / `刷新货架`
- **每人每日 5 次**上限，**每日第 1 次免费**
- 消耗随境界指数递增：`SHOP_REFRESH_COST_BASE × SHOP_REFRESH_COST_RATE^境界索引`
  （默认 `50 × 1.30^n`，与装备价格曲线同源）
- 每 6 小时的自动换货保留作为兜底

### 🛡 顶级物品保护

- **仙品 / 神品 / 混元先天永不上架**（配置项 `SHOP_TOP_TIER_RANKS`），
  只能通过世界 Boss、秘境等途径获取
- 货架最高品质 = 玩家当前可用品质，**没有越级槽**，不会刷出用不了的装备

### 🐛 顺带修复

- 修复商店配置项实际不生效：`PAVILION_REFRESH_HOURS`、`PAVILION_*_COUNT`、
  `SHOP_DISCOUNT_MIN/MAX`、`SHOP_STOCK_DIVISOR` 此前直接读顶层键，
  而配置实际存放在 `VALUES` 分组下，导致这些设置一直静默走默认值。
  现统一经 `ShopManager._cfg()` 优先从 `VALUES` 读取
- 修复购买失败提示让用户使用「市场」命令、但该命令并不存在的问题，
  改为提示查看当前货架短码或用 `刷新商店` 换货
- `丹阁 / 器阁 / 百宝阁` 现需要玩家数据，已加入忙碌状态（闭关/历练）白名单，
  闭关期间仍可浏览货架

### 📦 数据库变更

- 新增 **v32 迁移**：创建 `shop_refresh_usage` 表
  （`user_id + refresh_date` 复合主键，记录每人每日刷新次数），
  并清理旧的全局货架数据（`pill_pavilion` / `weapon_pavilion` / `treasure_pavilion`）
- `_create_all_tables_v2` 全新安装路径补齐该表；启动自检也会幂等创建

### ⚙️ 新增配置项（`VALUES` 分组）

| 配置项 | 默认值 | 说明 |
|--------|--------|------|
| `SHOP_REFRESH_DAILY_LIMIT` | 5 | 每人每日刷新次数上限 |
| `SHOP_REFRESH_FREE_TIMES` | 1 | 每日免费刷新次数 |
| `SHOP_REFRESH_COST_BASE` | 50 | 刷新消耗基数 |
| `SHOP_REFRESH_COST_RATE` | 1.3 | 刷新消耗随境界递增倍率 |
| `SHOP_FIT_ITEM_COUNT` | 2 | 货架契合槽数量（1~2 件） |
| `SHOP_FILL_TIER_RANGE` | 2 | 补充槽最多向下取几档 |
| `SHOP_FIT_TIER_WEIGHT_BONUS` | 3.0 | 契合槽品质抽取权重加成 |
| `SHOP_TOP_TIER_RANKS` | 仙品/神品/混元先天 | 永不上架的顶级品质 |

## v3.5.6 (2026-09-11) - 宗门每日任务跨天结算修复

### 🐛 Bug修复

#### 完成探索报 UNIQUE constraint failed: sect_daily_tasks.user_id

- 现象：宗门成员执行「完成探索」（伴随的还有完成历练、收获灵田、宗门捐献）时抛出
  `UNIQUE constraint failed: sect_daily_tasks.user_id`，指令直接失败，秘境奖励结算中断
- 根因：`sect_daily_tasks` 建表时把 **`user_id` 设为单主键**，而每日任务逻辑是按
  `(user_id, task_date)` 每天写入一行 —— 玩家当天首次能建记录，**次日**再建记录时就主键冲突。
  （v3.5.5 之前该问题被 `name 'datetime' is not defined` 报错掩盖，修好导入后才暴露出来）
- 修复：表结构改为 `PRIMARY KEY (user_id, task_date)`，同一玩家每天一行
  - 新增 **v31 数据库迁移**：自动重建老库为复合主键，保留 `(user_id, task_date)`
    与已有任务进度，幂等（结构正确时不做任何改动）
  - `SectManager` 写入改为 `INSERT ... ON CONFLICT(user_id, task_date) DO NOTHING`；
    若运行时检测到旧结构（缺列 / `ON CONFLICT` 不匹配），会**自动重建兜底**后重试
  - 任务标记改为原子更新 `UPDATE ... WHERE user_id=? AND task_date=? AND <任务列>=0`，
    以 `rowcount` 判断是否首次完成，避免并发或重复触发时重复发放贡献度
- 全新安装路径（`_create_all_tables_v2`）补齐 `sect_daily_tasks` 建表，
    不再出现「no such table」问题
  - 启动时新增**表结构自检**：即使 `db_info` 版本号已经是最新（手动改库、拷贝数据、
    迁移中途失败等情况），也会在插件启动时校验并修复该表结构
  - **每日任务结算改为静默降级**：结算本身异常时只记录日志、跳过本次奖励，
    不再让「收获」「完成探索」「历练」「宗门捐献」等主指令跟着失败
- 影响路径：探索秘境 / 完成历练 / 收获灵田 / 宗门捐献（≥1000 灵石）的每日任务自动结算，
  以及 `宗门任务` 面板

#### 购买等指令报 cannot start a transaction within a transaction

- 现象：`购买` 抛 `cannot start a transaction within a transaction`，而且**同一连接上会持续复现**
  ——随后存/取灵石、悬赏、灵眼抢占、炼化材料等所有 `BEGIN IMMEDIATE` 型指令一起失效
- 根因：上面那条 `UNIQUE constraint failed` 的 `INSERT` 在 SQLite 中**先隐式开启了事务**才失败，
  旧代码没有回滚，事务就一直挂在共享连接上；此后任何 `BEGIN IMMEDIATE` 都因
  「事务中不能再开事务」直接失败，形成连锁故障
- 修复：
  - 新增统一事务入口 `begin_immediate()`：检测到该错误时**自动回滚残留事务并重试**
    （购买、银行存取款、悬赏、灵眼、师徒、炼化、储物戒整理等 18 处 `BEGIN IMMEDIATE` 全部改走该入口）
  - 宗门每日任务的写入路径补充 `rollback` 保护，异常不再残留事务污染共享连接
- 影响路径：`购买`、`存灵石`/`取灵石`、`悬赏`、`灵眼`、`师徒`、`一键炼化`、储物戒整理等

### 📦 数据库变更

- **需要迁移**：新增 v31，自动重建 `sect_daily_tasks` 为 `(user_id, task_date)` 复合主键
  （插件启动时自动执行，无需手动操作；历史任务进度保留，不影响玩家数据）

### 📋 指令变更

- 无（未新增/调整指令）

---

## v3.5.5 (2026-09-10) - 指令冲突与功法装备修复

### 🐛 Bug修复

#### 赠予物品无法解析物品名

- 现象：`赠予 @某人 星辰炼体诀 1` 始终提示「请指定要赠予的物品名称」，物品送不出去
- 根因：AstrBot 的 `CommandFilter` 把指令后的消息按空格切分并**按位置绑定到函数形参**，
  形参只有一个时后续 token 全被丢弃；而 `@某人` 恰好占据首个 token，
  处理后仅剩「@某人」再被 @ 清理正则清空 → 物品名丢失
- 修复：`handle_gift_item()` 改用 `extract_command_args()` 从原始消息解析完整参数，
  并用 `utils.resolve_target_user_id()` 统一解析赠予对象（At 组件 → CQ 码 → 纯数字QQ → 道号/昵称）

#### 秘境掉落功法无法装备

- 现象：`装备 元婴变` 提示「未找到物品：元婴变」，秘境掉落的功法只能躺在储物戒里
- 根因：这些功法来自 `RIFT_SKILL_TABLE`（元婴变、血海魔功、星辰炼体诀…），
  未登记进 items.json / weapons.json，`EquipmentManager.parse_item_from_name()` 无法解析
- 修复：`RiftManager` 按秘境等级 + 功法流派生成完整功法配置（36 部功法）：
  - 细分类型含「心法」→ 主修心法（提供修为倍率、灵气/气血容量）
  - 肉身/防御类 → 物防/法防/气血；身法/空间/时间类 → 身法防御与精神力；其余 → 攻伐
  - `parse_item_from_name()` 增加掉落功法兜底解析，并补齐中文细分类型的映射
- 附带完善：`物品信息 <功法名>` 现在展示**属性加成**（含秘境掉落动态配置）

### 🔧 指令调整（避让第三方插件冲突）

- `装备` → **`修仙装备`**（别名 `装备物品`）：与钓鱼类插件的「装备」指令冲突，同一条消息会触发两个插件
- `卸下` 保持不变，另新增 `修仙卸下` 作为等价指令
- 帮助文案、物品信息提示、README 同步更新

### 📦 数据库变更

- 无需迁移

### 📋 指令变更

```
/修仙装备 <名称>     （原「装备」，避免与钓鱼插件冲突）
/装备物品 <名称>     （同上，别名）
/修仙卸下 <名称>     （新增，等价「卸下」）
/赠予 @某人|QQ号|道号 <物品名> [数量]   （修复解析）
```

---

## v3.5.4 (2026-09-10) - 帮助体系与宗门文档

### 🆕 新增功能

#### 系统详解帮助（17 个）

总览与分级帮助分离：`修仙帮助` 看全量指令，`<系统>帮助` 看该系统完整玩法与数值。

```
宗门帮助     - 职位权限、捐献、功法库、宗门秘境、每日任务
组队帮助     - 队伍管理、掉落加成、时间折扣、专属副本
秘境帮助     - 掉落概率、次数限制、秘境装备说明
炼丹帮助     - 配方、成功率与境界加成
灵田帮助     - 作物时长、灵田等级与升级费用
师徒帮助     - 收徒/拜师条件、灌顶、传道奖励、出师
悬赏帮助     - 难度分级、奖励倍率、专属掉落
历练帮助     - 四条路线与风险收益
洞天帮助     - 类型、价格、产出与升级
银行帮助     - 利率、贷款与逾期后果
丹药帮助     - 批量服用与各类上限规则
战斗帮助     - 切磋/决斗/世界Boss/传承挑战
储物戒帮助   - 分类、取出、赠予、炼化
灵眼帮助 / 双修帮助 / 传承帮助 / 排行帮助
```

- `修仙帮助` 支持指定系统：`修仙帮助 宗门`
- 支持别名匹配：门派 / 队伍 / 背包 / boss / 排行榜 等
- `修仙帮助` 总览末尾新增「系统详解」入口

#### 实现方式

- 新增 `handlers/help_handler.py`：数据驱动的帮助主题注册表（`HELP_TOPICS` + `TOPIC_ALIASES`），
  新增系统只需补一条数据，无需改动指令注册逻辑

### 📖 文档补充

- README 重写「宗门系统」章节，补充：
  - 职位权限对照表（宗主/长老/亲传/内门/外门）
  - 建设度 → 修炼加成对照表（+5%~+30%）
  - 宗门秘境门槛表（建设度 + 境界要求 + 贡献度消耗）
  - 宗门每日任务表与功法共享机制
  - ⚠️ 明确说明 `sect_materials` / `elixir_room_level`（丹房）/ `sect_fairyland`（宗门洞天）等
    为**数据结构预留字段，暂未开放玩法**（原文未说明，容易误解为已有功能）
- 修正原文档中宗门秘境的境界标注（实际为 金丹期初期 / 元婴期后期 / 合体期后期）
- README 新增「系统详解帮助」指令表

### 🐛 Bug修复

- 修复 `宗门任务` 被注册两次的问题：`CMD_SECT_TASK` 与 `CMD_SECT_TASKS` 取值相同，
  AstrBot 会把两个处理器都激活 → 一条指令回两条消息。
  现将旧的「执行宗门任务」重命名为 `宗门建设`，`宗门任务` 专用于每日任务面板

### 📦 数据库变更

- 无需迁移

### 📋 新增指令

```
/宗门帮助 /组队帮助 /秘境帮助 /炼丹帮助 /灵田帮助 /师徒帮助
/悬赏帮助 /历练帮助 /洞天帮助 /银行帮助 /丹药帮助 /战斗帮助
/储物戒帮助 /灵眼帮助 /双修帮助 /传承帮助 /排行帮助
/修仙帮助 <系统>
/宗门建设            （原「宗门任务」重命名）
```

---

## v3.5.3 (2026-09-10) - 丹药与装备修复

### 🐛 Bug修复

#### 批量服用丹药数量失效

- 现象：`服用丹药 凝气丹 10` 只服用 1 个
- 根因：AstrBot 的 `CommandFilter` 把消息按空格切分后**按位置绑定到函数形参**，
  函数只声明了一个 `pill_name` 形参，第二个 token（数量）被直接丢弃
- 修复：新增 `handlers/utils.py: extract_command_args()`，从原始消息重新解析完整参数；
  同时为指令补齐第二个形参（兼容各版本 AstrBot 的绑定行为）
- 同类问题一并修复（数量参数此前都会被吞掉）：
  - `炼化材料 <材料名> 全部/数量`
  - `取出 <物品名> 数量`
  - `丢弃 <物品名> 数量`
  - `购买 <物品名> 数量`

#### 旧版丹药无法服用

- 现象：秘境/商店掉落的 items.json 丹药（七品太乙丹、三品凝神增益丹等）进入丹药背包后
  提示「配置不存在」，永远无法服用
- 根因：`PillManager.get_pill_by_name()` 只查 pills/exp_pills/utility_pills 三个配置，
  而 `is_pill()` 却把 items.json 的「丹药」也算作丹药，导致掉落可入库但不可用
- 修复：`get_pill_by_name()` 兼容 items.json 丹药；新增 `apply_legacy_pill_effects()`
  统一结算旧版 `effect` 效果，商店「购买即服用」与背包服用共用同一套逻辑

#### 秘境掉落装备无法穿戴

- 现象：秘境掉落的 45 件装备（精铁剑、布衣、血魔剑…）只能炼化，`/装备` 提示未找到物品
- 根因：这些装备来自 `RIFT_EQUIPMENT_TABLE`，未登记进 items.json / weapons.json，
  `EquipmentManager.parse_item_from_name()` 无法解析
- 修复：`RiftManager` 按秘境等级/品质生成装备配置（攻击、防御、精神力、境界需求），
  `parse_item_from_name()` 增加掉落装备兜底解析

#### 宗门每日任务报错（name 'datetime' is not defined）

- 现象：宗门成员完成秘境探索时抛出 `name 'datetime' is not defined` 导致指令失败，
  `宗门任务` 面板同样报错
- 根因：v3.5.0 新增的宗门每日任务（`get_daily_tasks` / `complete_daily_task`）中使用了
  `datetime.now()`，但 `managers/sect_manager.py` 顶部只导入了 `random` / `time`，
  缺少 `from datetime import datetime`（同文件另一处是函数内局部导入，因此只有这两个方法报错）
- 修复：补充模块级 `datetime` 导入
- 影响路径：秘境探索完成、宗门捐献（≥1000灵石）、历练完成、灵田收获的每日任务自动结算

#### 宗门相关 AttributeError（我的信息 / 出关 / 传承挑战）

- 现象1：`我的信息`、`出关` 报 `'PlayerHandler' object has no attribute 'sect_mgr'`
- 现象2：宗门玩家存在过期借用功法时报 `'Player' object has no attribute 'get_techniques'`
- 现象3：`传承挑战` 报 `'CombatManager' object has no attribute 'calculate_combat_stats'`
- 根因：v3.5.0 的「借用功法到期检查」等新增代码引用了未初始化的属性与不存在的方法
  （`Player` 实际只提供 `get_techniques_list()` / `set_techniques_list()`；
  `CombatManager` 从未定义 `calculate_combat_stats()`）
- 修复：
  - `PlayerHandler.__init__` 初始化 `self.sect_mgr = SectManager(db, config_manager)`
  - `SectManager` 中 5 处方法名改为 `Player` 实际提供的 `get_techniques_list()` / `set_techniques_list()`
  - `ImpartPkManager` 新增 `_build_combat_stats()`，按切磋/决斗同一套公式（传承加成 + 装备加成）
    构建战斗属性，`main.py` 注入 `config_manager`
- 说明：触发条件为「已加入宗门」的玩家（`player.sect_id != 0`），因此无宗门玩家不受影响

### 🆕 功能优化

- **一键服用**（`一键服用` / `批量服用`）：按上限规则服用背包中所有可服丹药
  - 永久属性丹药：受当前境界属性上限（基础值 30% + 上限丹加成）限制，自动停止
  - 回生丹/定魂丹/临时效果丹药：同名效果生效时不重复服用（效果不叠加）
  - 突破增益类旧版丹药：1 小时限时效果，一键服用时仅服 1 个
  - 恢复类丹药：按能量缺口计算所需数量，避免溢出浪费
  - 结束输出：已服用 / 未能服用（含原因）明细
- **我的信息**：丹药区新增「永久属性上限」进度（如 `物伤 1500/5100（29%）`）与
  「丹药可服用情况」（如 `小力量丹×200 → 最多再服 72 个（受物伤上限限制）`）
- 秘境掉落装备的属性按秘境等级 1-8 生成，与同境界器阁装备数值对齐

### 📦 数据库变更

- 无需迁移：仅调整指令解析与丹药/装备结算逻辑

### 📋 新增指令

```
/一键服用          - 服用背包中所有可服丹药
/批量服用          - 同上（别名）
/服用丹药 <名> <数量> - 批量服用指定丹药（修复数量失效）
```

---

## v3.5.2 (2026-09-10) - 组队与储物戒优化

### 🐛 Bug修复

#### 组队邀请 @ 不到人

- 修复 `邀请入队 @某人` 提示「找不到玩家」的问题
- 原因：旧实现只在消息文本中匹配 `[CQ:at,qq=数字]`，AstrBot 的 At 消息链、部分适配器的
  XML at 标签、以及只留下 `@昵称` 的情形都无法解析
- 现在统一走 `handlers/utils.py: resolve_target_user_id()`：
  1. 消息链 At 组件（`qq` / `target` / `uin` / `user_id` / `openid`）
  2. CQ 码 / `<at>` 标签（含 `[CQ:at,qq=1,name=x]` 这类带额外参数的形式）
  3. 指令参数中的数字ID
  4. 道号/昵称（精确 → 模糊匹配），`@昵称` 也能正确识别
- 同一问题同步修复：`踢出队伍`、`分配物品`、`收徒/拜师/接受拜师/拒绝拜师/灌顶`、
  `双修`、`传承挑战`

#### 储物戒物品分类错误

- 修复秘境/Boss 掉落物品被塞进【其他】的问题，`精铁剑`（武器）此前甚至被误判为【材料】
- 新增 `core/item_registry.py` 统一物品索引，聚合：
  - 配置：items.json / weapons.json / pills.json / exp_pills.json / utility_pills.json /
    storage_rings.json / alchemy_recipes.json
  - 掉落表：秘境（材料/装备/功法）、世界Boss、历练、悬赏、灵田灵草
- 关键修复效果：`精铁剑`/`布衣`/`护心镜` → 【装备】，`玄冰晶`/`龙鳞`/`血魂石` → 【材料】，
  `基础剑诀`/`凝神诀` → 【功法】

### 🆕 功能优化

- **主动加入队伍**：新增 `加入队伍 <队伍编号>` 或 `加入队伍 @队长`，无需邀请即可加入
  - 校验：队伍存在、待命中、未满员、加入者未在其它队伍
  - 创建队伍与 `队伍信息` 现在显示队伍编号
- **储物戒物品信息补全**：物品后标注类型/品质/细分（如 `精铁剑（武器）`、`青锋剑（法器·凡品）`、
  `仙器碎片×2（可炼化⚗️）`），未登记物品标注「未登记物品」
- **物品信息指令增强**：`物品信息 <名称>` 现在支持查询秘境/Boss 掉落物品的类型、品质、
  获取途径与用途提示（此前只能查询商店上架物品）
- `搜索物品` 结果同步显示物品类型标注

### 📦 数据库变更

- 无需迁移：加入队伍复用 `teams` / `team_members` / `team_invitations` 表

### 📋 新增指令

```
/加入队伍 <队伍编号>   - 主动加入指定队伍
/加入队伍 @队长        - 主动加入该队长的队伍
```

---

## v3.5.1 (2026-09-10) - 一键炼化（功能优化）

### 🔧 功能优化

- **一键炼化**：新增指令 `一键炼化`，把储物戒中所有可炼化材料一次性炼化，无需再逐个手动输入
  - 别名指令：`炼化全部`
  - 输出每种材料明细（`【材料】×数量 → 灵石+X 修为+Y`）与总量汇总
- **炼化图鉴升级**：`炼化图鉴` 现在会列出你当前持有的可炼化材料、各自收益与合计收益，并提示一键炼化

### 🛡️ 健壮性改进

- 新增数据库方法 `refine_storage_materials_batch`，批量炼化在单个 `BEGIN IMMEDIATE` 事务内完成，
  材料扣除与灵石/修为发放同时成功或同时回滚（含 `MIN(gold, 2^63-1)` 溢出保护）
- 储物戒物品数量读取（`get_item_count` / 单材料炼化）兼容 `int` 与 `{count, bound}` 两种历史存储格式，
  避免脏数据导致炼化报错

### 📦 数据库变更

- 无需迁移：复用现有 `players.storage_ring_items` / `gold` / `experience` 字段

### 📋 新增指令

```
/一键炼化   - 一次性炼化储物戒中全部可炼化材料
/炼化全部   - 同上（别名）
```

## v3.5.0 (2026-09-09) - 宗门福利升级

### 🔧 功能改进

#### 📚 宗门功法库（新增）

- `宗门功法库` - 查看宗门功法库列表
- `捐献功法 <功法名>` - 宗主/长老捐献功法（+100 贡献度）
- `借用功法 <功法名>` - 借用功法 7 天，最多同时借用 2 个
- 借用的功法自动装备，到期自动归还；查看信息/出关时会检测并归还过期功法

#### 📋 宗门每日任务（新增）

- `宗门任务` - 查看今日任务完成情况
- 💰 捐献灵石（≥1000，+50 贡献度）
- 🚶 完成历练（+30 贡献度）
- 🌾 收获灵田（+20 贡献度）
- 🌀 探索秘境（+40 贡献度）
- 任务自动检测完成，无需手动提交

#### 🎁 宗门福利

- 📈 修炼加成：按宗门建设度获得 5%-30% 修炼速度加成
- 🌀 宗门秘境：专属秘境奖励 1.5 倍，建设度 10000/30000/60000+ 解锁 3 档

### 📦 数据库变更

- 数据库版本升级至 v30
- `sect_technique_borrow` 新增 `is_active` 字段，并新增 `(user_id, is_active)` 索引
- `sect_daily_tasks` 表结构重建为独立字段（`donated_stone` / `completed_adventure` /
  `harvested_farm` / `completed_rift`），并新增 `task_date` 索引

### 📋 新增指令

```
/宗门功法库           - 查看宗门功法库列表
/捐献功法 <功法名>    - 宗主/长老捐献功法
/借用功法 <功法名>    - 借用功法 7 天
/宗门任务             - 查看宗门每日任务
```

---

## v3.4.1 (2026-09-09) - 师徒系统优化

### 🔧 功能改进

- **拜师机制优化**：改为请求-接受模式，需要师父/徒弟双方同意才能建立师徒关系
  - `收徒 @某人` - 发送收徒邀请
  - `拜师 @某人` - 发送拜师请求
  - `接受拜师 [@某人]` - 接受拜师请求
  - `拒绝拜师 [@某人]` - 拒绝拜师请求

### 🐛 Bug修复

- **修复灌顶逻辑**：支持双向灌顶（师父灌顶徒弟 或 徒弟灌顶师父）
- 修复徒弟灌顶师父时提示"你们不是师徒关系"的错误

### 📦 数据库变更

- 新增 `mentorship_requests` 表记录拜师请求
- 数据库版本升级至 v29

---

## v3.4.0 (2026-09-09) - 组队秘境系统

### 🆕 新功能

#### 组队秘境系统

多人协作探索高难度副本，增强社交互动和游戏深度。

**核心功能：**

- ✅ 队伍创建与管理（2-4人）
- ✅ 邀请系统（5分钟有效期）
- ✅ 队长权限管理（踢人、解散、分配）
- ✅ 组队探索机制
- ✅ 掉落分配系统

**组队机制：**

- 🎯 **准入规则**：队伍平均等级 ≥ 秘境要求等级
- ⏱️ **时间折扣**：2人90%、3人80%、4人70%
- 💎 **掉落加成**：2人+20%、3人+35%、4人+50%
- 📈 **经验分配**：所有队员100%经验（不分摊）
- 🎁 **传承加成**：低等级队员额外+30%-50%经验
- 🔢 **次数限制**：每人每日5次（单人和组队共享）

**组队专属秘境：**

1. **修罗战场** (ID:16)
   - 等级：元婴期 (16)
   - 人数：2-4人
   - 特色：极品装备、传说功法、稀有材料

2. **九幽深渊** (ID:17)
   - 等级：化神期 (22)
   - 人数：3-4人
   - 特色：传说装备、神器、顶级材料

3. **天道试炼** (ID:18)
   - 等级：炼虚期 (28)
   - 人数：4人满编
   - 特色：神器装备、至尊功法、仙级材料

### 📋 新增指令

```
/创建队伍          - 创建新队伍
/邀请入队 @某人    - 邀请玩家加入
/接受组队          - 接受邀请
/拒绝组队          - 拒绝邀请
/队伍信息          - 查看队伍状态
/离开队伍          - 离开队伍
/踢出队伍 @某人    - 队长踢出队员
/解散队伍          - 队长解散队伍
/组队探索 <ID>     - 队长发起探索
/完成组队          - 队长完成探索
/队伍掉落          - 查看待分配物品
/分配物品 <编号> @某人 - 队长分配掉落
```

### 🔧 技术改进

- **数据库版本**：升级至 v28
- **新增表**：teams、team_members、team_invitations、team_loot
- **新增管理器**：TeamManager
- **新增处理器**：TeamHandlers
- **扩展管理器**：RiftManager 支持组队探索

### 📖 文档更新

- ✅ 更新 README.md 添加组队系统说明
- ✅ 更新游戏内帮助文档（/修仙帮助）
- ✅ 创建实现总结文档

### 🎮 游戏平衡

**携带机制：**

- 允许高等级玩家带低等级玩家
- 基于队伍平均等级准入，公平合理
- 低等级玩家获得额外经验加成

**防刷机制：**

- 个人次数制：每人每日5次
- 单人和组队共享次数
- 防止频繁换队无限刷

**掉落优化：**

- 组队掉落率加成鼓励协作
- 等级差过大惩罚掉落
- 队长分配保证公平性

---

## v3.3.0 (2026-09-08) - 师徒系统

### 🆕 新功能

- ✅ 师徒系统
- ✅ 收徒与拜师
- ✅ 灌顶功能
- ✅ 传道奖励
- ✅ 出师奖励

---

## v3.2.3 (2026-09-07)

### 🐛 Bug修复

- 修复秘境系统道具丢失的严重bug
- 优化攻击力计算公式
- 修正灵修与体修的伤害加成逻辑

---

## v3.2.0

### 🆕 新功能

- ✅ 批量服用丹药功能
- ✅ 批量种植灵草功能

---

更多历史版本请查看 Git 提交记录。
